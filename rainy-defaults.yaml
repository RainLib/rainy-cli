apiVersion: rainy.dev/v1
kind: RainyDefaults

metadata:
  name: rainy-official
  version: 0.5.3

requires:
  rainy: ">=0.5.0, <0.6.0"

paths:
  packs: community-packs
  skills: integrations/skills
  templates: defaults/templates
